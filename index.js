/*


function getValue(str) {
  return "Вы ввели" + " " + str;
}
console.log(getValue("Вот такой вот текст"));

function getSum(num) {
  if (typeof num === "number") return num + 5;
  console.log("Ошибка! Вы ввели не число!");
  return;
}
console.log(getSum(3));

function getStr(str, str2) {
  if (typeof str === "string") return str + " " + str2;
  console.log("Ошибка! Вы ввели не строку!");
  return;
}
console.log(getStr("Privet", "Vasya"));

function getSumofTwo(numb, numb2) {
  if (typeof numb === "number") return numb * numb2;
  console.log("Ошибка! Вы ввели не число!");
  return;
}
console.log(getSumofTwo(2, 2));

function checkRemofDiv(val1, val2) {
  if (val1 % val2 == 0) {
    return false;
  } else {
    return true;
  }
}
console.log(checkRemofDiv(2, 2));

function getNumofDegree(val, val2) {
  return Math.pow(val, val2);
}
console.log(getNumofDegree(2, 16));

function getArea(a, b, c) {
  let p = (a + b + c) / 2;
  let pl = Math.sqrt(p * (p - a) * (p - b) * (p - c));
  return pl;
}
console.log(getArea(10, 10, 10));

// // //сделал на ифах, со свитчем еще не до конца разобрался
function getResult(a, o, b) {
  if (o == "+") {
    console.log(a + b);
  }
  if (o == "-") {
    console.log(a - b);
  }
  if (o == "+") {
    console.log(a + b);
  }
  if (o == "*") {
    console.log(a * b);
  }
  if (o == "/") {
    console.log(a / b);
  }
}
console.log(getResult(50, "/", 10));

function getMessage() {
  for (let i = 0; i < 5; i++) {
    console.log("Hello Loop");
  }
}
getMessage();

function getMessageAndI(i) {
  for (let i = 0; i < 5; i++) {
    console.log("Hello Loop" + i);
  }
}
getMessageAndI("i");

function getNum(num) {
  for (let i = 0; i < 5; i++) {
    console.log(num);
  }

  return num;
}
console.log(getNum("i"));

function sumOfTwo(a, b) {
  for (let i = 0; i < 5; i++) {
    console.log(a * b);
  }
}
console.log(sumOfTwo(5, 8));

function retBuffer(num) {
  let buffer = 1;

  for (let i = 5; i < 15; i++) {
    return i * buffer;
  }
}
console.log(retBuffer(5));

function retBuff(num) {
  let buffer = 1;

  for (let i = 1; i <= num; i++) {
    buffer *= i;
  }

  return buffer;
}
console.log(retBuff(8));

*/

// Последние два задания не разобрался, пересмотрю еще материалы и досдам

/*
let giraffe = {
    age : 20,
    weight : 400,
    heightTorso :200,
    neckLength : 100, 
    height : heightTorso + neckLength,
   
    sleepState : false,
}

//Object.defineProperty(giraffe, sleepState, true)

*/


